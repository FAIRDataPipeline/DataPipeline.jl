name: CI

concurrency:
  group: ${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
  workflow_dispatch:

jobs:
  pipeline-tests:
    strategy:
      matrix:
        julia-version:
          - '1.11'
          - '1'
        os:
          - ubuntu-latest
          - macOS-latest
          - windows-latest
      fail-fast: false
    uses: ./.github/workflows/pipeline-tests.yaml
    with:
      julia-version: ${{ matrix.julia-version }}
      os: ${{ matrix.os }}
    secrets: inherit
